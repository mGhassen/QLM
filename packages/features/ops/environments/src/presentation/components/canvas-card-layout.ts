/**
 * Grid = ordered slots (reorder). Free = absolute positions on an open canvas.
 * Timeline = horizontal checkpoints with stacked clones (service workspace).
 */
export type CanvasCardLayoutMode = "grid" | "free" | "timeline";
